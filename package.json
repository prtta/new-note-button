{
  "name": "new-note-button",
  "version": "0.2.1",
  "private": true,
  "description": "A floating new-note button for Obsidian on iOS.",
  "author": "prtta",
  "license": "MIT",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.7",
    "typescript": "^5.8.0"
  }
}
